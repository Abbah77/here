enum PostType {
  text,           // Regular text post
  image,          // Single image post
  multiImage,     // Multiple images (like second.dart)
  video,          // Video post with play button
  link,           // Link/article post (like fourth.dart)
  checkIn,        // Location check-in with map (like third.dart)
  connection,     // Connection announcement (like first.dart)
}